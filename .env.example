# Copia questo file in ".env" e inserisci il tuo API Read Access Token (v4) di TMDB.
# Il file ".env" NON viene committato (è escluso in .gitignore).
# La variabile deve avere il prefisso NG_APP_ per essere letta da Angular.

NG_APP_TMDB_TOKEN=
